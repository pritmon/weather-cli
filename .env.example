# weather-cli uses Open-Meteo — no API key is required.
# This file is intentionally empty and exists only as a template placeholder.
# If you fork this project and add a provider that needs keys, add them here.

# WEATHER_API_KEY=your_key_here
